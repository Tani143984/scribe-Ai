// app/layout.tsx
// The root layout wraps EVERY page in the app.
// We put the SessionProvider here so useSession() works everywhere.

import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";
import { Providers } from "./providers";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "ScribeAI — AI Writing Assistant",
  description: "Generate blog posts, emails, and LinkedIn content with AI",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <Providers>{children}</Providers>
      </body>
    </html>
  );
}
